{
  "rules": {
    "presence": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "presenceNames": {
      ".read": "auth != null && (auth.token.email === 'admin.med101@gmail.com' || auth.token.email === 'admin1.med101@gmail.com')",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
